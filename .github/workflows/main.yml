# .github/workflows/main.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
      - name: Run Tests
        run: pytest --maxfail=1 --disable-warnings -q
      - name: Build Docker Image
        run: docker build -t weather-pipeline:latest -f docker/Dockerfile .
  deploy:
    runs-on: ubuntu-latest
    needs: build_and_test
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      # Example deployment step – customize with gcloud CLI commands and service account secrets.
      - name: Deploy to Cloud Run (placeholder)
        run: echo "Deploying to Cloud Run – configure gcloud CLI with authentication and target service."
